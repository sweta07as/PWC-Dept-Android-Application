<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/pwclogo_removed"
        android:label="@string/app_name"
        android:roundIcon="@drawable/pwclogo_removed"
        android:supportsRtl="true"
        android:theme="@style/Theme.FirstAddOnCourseAndroidApplication"
        tools:targetApi="31">


        <activity
            android:name=".ScienceDepartment"
            android:exported="false"/>

        <activity
            android:name=".CommerceDepartment"
            android:exported="false" />

        <activity
            android:name=".ArtsDepartment"
            android:exported="false" />

        <activity
            android:name=".Department"
            android:exported="false" />

        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity
            android:name=".MainActivity"
            android:exported="false"/>


        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>